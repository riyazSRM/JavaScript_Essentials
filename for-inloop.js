let names = new Array("Alice", "Bob", "Charlie");   
console.log("Using for-in loop to iterate over an array:");
//print array elements using for-in loop
for (let index in names) {  
    console.log(`Name at index ${index} is ${names[index]}`);
}   

